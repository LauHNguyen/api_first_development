version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install axios
  build:
    commands:
      - echo Running user API tests...
      - node test/user-api.test.js
